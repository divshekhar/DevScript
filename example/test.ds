var x = 10;
var y = 20;

// function declaration
func add(x, y) {
    return x + y;
}

// Conditional function call
var res = if(x > y) { x; } else { add(x, y); }

println(add(res,y));
println(len("Hello World!"));
